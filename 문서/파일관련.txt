glob -> standard lib
files = glob("./data/**/**/*") *.txt *.csv 특정 확장자만.
jpg jpeg JPG 는 하나로 맞추기.

데이터 파일 받으면 일단 열어 본다.
지우려는 row -> skiprows=몇줄인지 수

missingno 라이브러리

1. 파일이 깨끗하게 오지 않을것이다. txt glob /10개미만 그냥 하나씩 하자/ -> 판다스로 df 형태로 변경, 원본은 손대지 않는다.

reduce 교집합
concat 병합
axis=0 옆으로?

덴스 하나만 만들어서 넣어


# DA
1. 데이터를 pandas dataFrame으로 변경
2. 전처리 적당히.
3. 독립변수, 종속변수 설정
4. 선형모델 만들기(싸이킷런으로) 싸이킷런으로 모델 저장.검ㅅ개해서 진솔 킴 꺼 참고
5. 플라스크를 이용해서 수단과 방법을 가리지 말고 "입력" 하고 "출력되게"
6. 입력은 하드코딩(결과값 미리 넣어두기-하드코딩)
7. 예측 결과가 나와야 됨. 